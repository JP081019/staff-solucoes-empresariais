import { CheckCircle2 } from "lucide-react";
import { Reveal } from "@/components/Reveal";
const points = ["Atendimento próximo e personalizado", "Profissionais selecionados para cada necessidade", "Soluções integradas em um só parceiro", "Compromisso com eficiência e qualidade"];
export function DifferenceSection() { return <section id="diferenciais" className="section difference"><div className="shell difference-grid"><Reveal className="difference-title"><span className="kicker light">Por que a Staff</span><h2>Confiança se constrói<br/><em>em cada detalhe.</em></h2></Reveal><div className="difference-list">{points.map((point, index) => <Reveal key={point} delay={index * 80} className="difference-item"><span>0{index + 1}</span><p>{point}</p><CheckCircle2/></Reveal>)}</div></div></section>; }
